name: TensoRF
channels:
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
    - configargparse
    - imageio-ffmpeg
    - pygltflib
    - kornia
    - lpips
    - opencv-python
    - plyfile
    - scikit-image
    - tensorboard
    - tqdm  